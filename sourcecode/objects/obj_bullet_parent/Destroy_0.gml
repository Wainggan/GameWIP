var deathBuffer = 64;


if (x < -deathBuffer || WIDTH+deathBuffer < x) || (y < -deathBuffer || HEIGHT+deathBuffer/2 < y) {
} else {
	//particle.burst(x, y, "bulletExplosion", (x_vel + lengthdir_x(spd, dir)), (y_vel + lengthdir_y(spd, dir)))
}
