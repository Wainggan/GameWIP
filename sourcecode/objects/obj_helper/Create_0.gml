x_target = x;
y_target = y;

x_anim = new Sod(2, 0.6, -1).setValue(obj_player.x);
y_anim = new Sod(2, 0.5, -1).setValue(obj_player.y);

tReloadTime = 12;
reloadTime = tReloadTime;

command_setup()


off = random(200);
spdOff = random(70)
dir = choose(-1, 1);

image_alpha = 1